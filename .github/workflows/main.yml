name : CI CD React native Deployment
on :
    push :
        branches: main
        
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            
            - name: setup node version
              uses: actions/setup-node@v4
            
              with: 
                node-version: 18

            - name: Install dependency
              run: echo "Intalling dependency"
              




          
